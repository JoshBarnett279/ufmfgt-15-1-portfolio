START
  PRINT "Menu:"
  PRINT "1. Check speed"
  PRINT "2. Convert m/s to mph"
  PRINT "3. Exit"

  READ choice

  SWITCH choice
    CASE 1:
      READ speed
      IF speed < 0 THEN
          PRINT "Invalid speed"
      ELSE IF speed > 70 THEN
          PRINT "Speeding"
      ELSE
          PRINT "Valid speed"
      ENDIF
    CASE 2:
      READ speed_ms
      speed_mph = speed_ms × 2.23694
      PRINT speed_mph
    CASE 3:
      PRINT "Exiting program"
    DEFAULT:
      PRINT "Invalid option"
  ENDSWITCH
END
