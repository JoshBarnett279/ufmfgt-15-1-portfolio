START

READ mark

IF mark < 0 OR mark > 100 THEN
    PRINT "Invalid"
ELSE IF mark < 40 THEN
    PRINT "Fail"
ELSE IF mark < 60 THEN
    PRINT "Pass"
ELSE IF mark < 70 THEN
    PRINT "Merit"
ELSE
    PRINT "Distinction"
END IF

ENDso