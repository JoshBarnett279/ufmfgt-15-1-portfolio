START

Define a functions called add_number
It takes two integers
It returns their sum

In main:
Declare two integer variables
Ask the user to input their first integer
Ask the user to enter the second integer
Call add_numbers using those values
Store the returned resalt
Print the result

END